// Use ESM syntax if type: "module" in package.json, otherwise use CJS (module.exports)
// Assuming type: "module" based on original package.json
export default {
    plugins: {
      tailwindcss: {},
      autoprefixer: {},
    },
  }